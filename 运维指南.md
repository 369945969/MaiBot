# MaiBot 使用与运维指南

## 快速启动

```bash
./start.sh
```

该脚本会自动检测并终止已运行的 MaiBot 实例，然后重新启动。

## 启动方式详解

### 方式一：start.sh 脚本（推荐）

```bash
./start.sh
```

特点：
- 自动检测并终止已有实例（优雅终止 → 强制终止）
- 使用 `uv run` 确保依赖环境正确
- 输出日志到 `logs/maibot.log`
- 记录 PID 到 `.maibot.pid` 文件

### 方式二：直接启动

```bash
uv run python bot.py
```

这种方式直接在前台运行，适合调试场景。按 Ctrl+C 可中断退出。

### 方式三：手动后台启动

```bash
nohup uv run python bot.py >> logs/maibot.log 2>&1 &
```

手动后台运行，需要自行管理进程。

## 进程模型

MaiBot 采用 Runner/Worker 双进程模型：

- **Runner 进程**：守护进程，负责启动和监控 Worker 进程
- **Worker 进程**：实际执行业务逻辑的进程

当 Worker 因退出码 42（重启请求）退出时，Runner 会自动重新启动 Worker。这意味着：
- Runner 进程的 PID 记录在 `.maibot.pid` 中
- Worker 进程由 Runner 管理，无需手动干预
- 正常重启（退出码 42）会在 Runner 层自动完成

## 停止服务

### 优雅停止

```bash
kill -TERM $(cat .maibot.pid)
```

发送 SIGTERM 信号，进程会尝试优雅退出。

### 强制停止

```bash
kill -9 $(cat .maibot.pid)
```

仅在优雅停止无效时使用。

### 查找并终止所有相关进程

```bash
pgrep -f "python.*bot.py" | xargs kill -TERM
```

## 查看运行状态

```bash
# 检查 PID 文件中的进程是否存活
ps -p $(cat .maibot.pid) -o pid,ppid,stat,command

# 查找所有 MaiBot 相关进程
pgrep -af "python.*bot.py"
```

## 日志管理

日志文件位于 `logs/` 目录：

| 文件 | 说明 |
|------|------|
| `logs/maibot.log` | start.sh 启动时的输出日志 |
| `logs/` 内其他文件 | MaiBot 内部日志系统生成的日志 |

查看实时日志：

```bash
tail -f logs/maibot.log
```

## 依赖管理

MaiBot 使用 `uv` 作为依赖管理工具。

### 安装/更新依赖

```bash
uv sync
```

### 添加新依赖

```bash
uv add <package-name>
```

添加后需同步更新 `requirements.txt`：

```bash
uv pip compile pyproject.toml -o requirements.txt
```

## 配置文件

| 文件 | 说明 |
|------|------|
| `bot_config.toml` | Bot 主配置（不要直接修改，修改模板） |
| `model_config.toml` | 模型配置（不要直接修改，修改模板） |
| 配置模板文件 | 位于 `src/config/templates/` 目录 |

修改配置时，只需修改模板文件并新增版本号，不需要直接修改实际配置文件。

## 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `MAIBOT_LOCALE` | 语言设置 | `zh-CN` |
| `MAIBOT_WORKER_PROCESS` | 标记为 Worker 进程（内部使用） | 无 |

## 常见问题

### 启动失败

1. 检查 Python 版本是否 >= 3.12
2. 检查依赖是否完整：`uv sync`
3. 查看日志：`cat logs/maibot.log`

### 进程残留

如果 start.sh 报告无法终止旧进程：

```bash
# 手动查找并终止
pgrep -af "python.*bot.py"
kill -9 <pid>

# 清理 PID 文件
rm -f .maibot.pid
```

## 端口配置

MaiBot 默认使用 6xxx 系列端口，避免与其他常见服务（如 HTTP 代理 8080、AirPlay 8000 等）冲突：

| 端口 | 服务 | 配置项 |
|------|------|--------|
| 6000 | 消息服务 (WebSocket/API) | `maim_message.ws_server_port` |
| 6001 | WebUI 管理面板 | `webui.port` |
| 6090 | API Server | `maim_message.api_server_port` |

> 如果从旧版升级，配置文件中的端口会通过升级钩子自动迁移（8000→6000, 8001→6001, 8090→6090）。

如果 WebUI 或其他服务端口被占用，可在 `bot_config.toml` 中修改对应配置项。

## Docker 部署

```bash
docker compose up -d
```

Docker 方式部署会自动管理进程生命周期，不需要使用 start.sh。